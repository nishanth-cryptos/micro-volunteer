node_modules/
dist/
build/

# Env files — never commit values
.env
.env.local
.env.*.local

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
functions/lib/
functions/node_modules/

# Emulator state snapshot (persists Auth users + Firestore docs between
# restarts via `npm run emulators`). Local-only; never commit.
emulator-data/

# Editors / OS
.DS_Store
.vscode/
.idea/
*.swp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
